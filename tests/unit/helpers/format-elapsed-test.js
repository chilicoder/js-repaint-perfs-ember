
import { formatElapsed } from 'js-repaint-perfs-ember-2-9-1/helpers/format-elapsed';
import { module, test } from 'qunit';

module('Unit | Helper | format elapsed');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatElapsed([42]);
  assert.ok(result);
});

