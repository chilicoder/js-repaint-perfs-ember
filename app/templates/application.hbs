<div>
    <table class="table table-striped latest-data">
        <tbody>
        {{#each model.databaseArray key='dbname' as |db|}}
            {{dbmon-database db=db}}
        {{/each}}
        </tbody>
    </table>
</div>